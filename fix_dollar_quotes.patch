--- src/executor/dollar_quotes_fix.c.orig
+++ src/executor/dollar_quotes_fix.c
@@ -25,6 +25,16 @@
 {
     int     start;
     int     len;
+    int     next_char;
+    char    *res;
+    char    *name;
+
+    if (!input || !i)
+        return (NULL);
+    
+    // Check for $"string" pattern which should be treated literally
+    next_char = *i + 1;
+    if (input[next_char] == '"') {
+        *i = *i + 1; // Skip the $, leave the " for normal processing
+        return (ft_strdup(""));
+    }
+    
     char    *res;
     char    *name;
